 package Media;

/**
 * The Visual interface represents a visual media item.
 * It extends the Media interface.
 */
public interface Visual extends Media {
    /**
     * Shows or displays the visual media item.
     */
    void show();
}